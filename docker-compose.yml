version: "3.5"

services:

  frontend:
    build:
      context: ./frontend/
    command: "npm run prod"
    networks:
      - front-tier

  backend:
    build:
      context: ./backend/
    command: "flask run --host=0.0.0.0"
    networks:
      - back-tier
    restart: always
  mongo:
    image: mongo
    command: --serviceExecutor adaptive
    restart: always
    networks:
      - back-tier

  redis:
    image: 'redis'
    networks:
      - back-tier

  nginx:
    restart: always
    build:
      context: ./nginx/
    ports:
    - "80:80"
    - "5000:5000"
    networks:
      - front-tier
      - back-tier


networks:
  front-tier:
  back-tier: